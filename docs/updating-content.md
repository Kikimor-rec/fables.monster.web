# Updating Content JSON

Text displayed on the site and in the terminal is stored under `public/content`.
To update terminal logs or interface strings, edit `terminal-content.json`.
New log entries will be appended automatically when the page reloads.
For general site copy, edit `site-content.json`.
