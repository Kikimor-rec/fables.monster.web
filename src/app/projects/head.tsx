import { createMetadata } from "@/lib/seo";
export const metadata = createMetadata(
  "Projects | Fables Monster Studio",
  "Overview of projects created by Fables Monster Studio.",
  "/projects",
);
export default function Head() {
  return null;
}
