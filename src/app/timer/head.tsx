import { createMetadata } from "@/lib/seo";
export const metadata = createMetadata(
  "Chronometer | Fables Monster Studio",
  "Digital countdown timer with retro terminal look.",
  "/timer",
);
export default function Head() {
  return null;
}
