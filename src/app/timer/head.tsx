import { createMetadata } from "@/lib/seo";

export const metadata = createMetadata({
  title: "Chronometer | Fables Monster Studio",
  description: "Digital countdown timer with retro terminal look.",
  path: "/timer",
  image: "/opengraph-image",
});

export default function Head() {
  return null;
}
